import styled from '@emotion/styled';
import React, { FunctionComponent } from 'react';

const RecomendationsPage: FunctionComponent = () => (
  <Recomendations>RecomendationsPage</Recomendations>
);

const Recomendations = styled.div`
  height: 100%;
`;

export default RecomendationsPage;
