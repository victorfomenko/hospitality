import React, { FunctionComponent } from 'react';
import { ReactComponent as Icon } from './img/adress.svg';

const AdressIcon: FunctionComponent = props => {
  return <Icon {...props} />;
};

export default AdressIcon;
